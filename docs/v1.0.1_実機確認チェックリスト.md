# Field Cross Search v1.0.1 hotfix 実機確認チェックリスト

最終更新: 2026-09-18
対象コード: main 0fe9db6 + 未コミット変更（source/js/desktop.js, source/js/config.js, source/config.html, README.md）
確認用 ZIP: `build/kintone-field-cross-search-plugin-v1.0.1-test.zip`（manifest version は 1.0.0 のまま。アイコンは現正式版と同一。plugin ID は現正式版と同じ）
ローカルテスト: 104 PASS / 0 FAIL

各項目の「結果」に PASS / FAIL / 未確認 を記入する。FAIL は末尾の「FAIL 記録」に書く。

> プラグインの更新は kintone 環境全体に効く。開発用環境での実施を推奨。戻す場合は現正式 ZIP（`field-cross-search-plugin.zip`、v1.0.0）を再読み込みする。
> どちらもバージョン表示は 1.0.0。検索バーに「AND | OR」ボタンがあれば確認用 ZIP が有効になっている。

## 優先順位

```
最優先: A 再発火 / D AND・OR / B IME / E URL復元 / F 選択肢系
次点  : C 語数上限 / H 検索不能語 / I snapshot不一致 / J 適用中表示 / L 設定画面
その他: G RICH_TEXT / K 0件 / M a11y・UI
```

---

## 0. 事前準備

検証アプリのフィールド（コード＝名前）:

| フィールド | 種類 | 用途 |
|---|---|---|
| 顧客名 | 文字列（1行） | 全般 |
| 備考 | 文字列（複数行） | 全般、I（後で削除） |
| 説明 | リッチエディター | G |
| 契約区分 | ドロップダウン（新規／更新／解約） | F |
| 優先度 | ラジオボタン（高／中／低） | F、I（後で選択肢「低」を削除） |
| 対応エリア | チェックボックス（東京都／神奈川県） | F |
| タグ | 複数選択（重要／保留） | F |
| 契約書 | 添付ファイル | 従来機能 |
| 明細（品名） | テーブル内 文字列（1行） | 従来機能 |
| 金額 / 契約開始日 / 担当者 | 数値 / 日付 / ユーザー選択 | L（候補に出ないこと） |

テストデータ: 25 件以上（一覧の表示件数を 20 にしてページングを発生させる）。
「東京」だけを含む／「営業」だけを含む／両方を含む／どちらも含まない レコードを各 2 件以上用意する（AND と OR の結果差を見るため）。
一覧を 2 つ以上（例:「すべて」「東京のみ」）用意する。

```
[ ] 0-1 確認用 ZIP を「更新」で読み込めた（プラグイン一覧に 1 件のまま）                          結果: (      )
[ ] 0-2 設定画面で 顧客名・備考・説明・契約区分・優先度・対応エリア・タグ を対象にして保存
        （AND / OR の初期値 = AND、検索語の上限 = 5）                                          結果: (      )
[ ] 0-3 一覧画面ヘッダーに [入力欄][AND|OR][検索][対象：7件] が表示される                         結果: (      )
```

DevTools（F12）の Console を開いた状態で実施する。

---

## A. index.show 再発火（最優先）

次の各操作のあと、**ページを手動リロードせずに** 検索 UI を操作する。

| 操作 | 内容 |
|---|---|
| A-a 一覧切替 | 「すべて」→「東京のみ」 |
| A-b ページング | 次のページへ |
| A-c ソート | 列見出しをクリック |
| A-d 絞り込み | kintone 標準の絞り込みを適用 |
| A-e 一覧再表示 | 絞り込み解除、ブラウザの戻る／進む |

```
[ ] A-1 A-a 後: 検索ボタンで検索できる                                                     結果: (      )
[ ] A-2 A-a 後: Enter で検索できる                                                         結果: (      )
[ ] A-3 A-b 後: 検索・クリアができる                                                       結果: (      )
[ ] A-4 A-c 後: 検索・クリアができる                                                       結果: (      )
[ ] A-5 A-d 後: 検索できる                                                                結果: (      )
[ ] A-6 A-e 後: 検索できる                                                                結果: (      )
[ ] A-7 上記の各操作後も AND / OR の切り替えができる                                         結果: (      )
[ ] A-8 上記の各操作後も「対象：N件」でパネルが開閉する                                       結果: (      )
[ ] A-9 二重実行されない（検索バーが 2 つ出ない、1 回の操作で 2 回読み込まれない。
        Network タブで一覧の読み込みが 1 回であること）                                       結果: (      )
[ ] A-10（参考）各操作がページ全体の再読み込みだったか、部分更新だったかをメモ                   結果: (      )
```

## B. IME（最優先）

```
[ ] B-1 「とうきょう」→ 変換 → Enter で確定した時点では検索しない                              結果: (      )
[ ] B-2 確定後の通常 Enter で 1 回だけ検索される                                            結果: (      )
[ ] B-3 半角英数字では Enter 1 回で検索される                                               結果: (      )
[ ] B-4 （可能なら）Chrome / Edge / Safari で同じ挙動                                        結果: (      )
```

## C. 5語 / 6語（次点）

```
[ ] C-1 「1 2 3 4 5」→ 「検索中: 1 2 3 4 5 / …」と 5 語すべてが表示される                       結果: (      )
[ ] C-2 「1 2 3 4 5 6」→ 「検索中: 1 2 3 4 5 / …」（6 語目は検索対象外）                        結果: (      )
[ ] C-3 同時に「検索語は最大5件までです。6件目以降は検索対象になりません：6」が表示される          結果: (      )
[ ] C-4 入力欄には「1 2 3 4 5 6」がそのまま残っている                                         結果: (      )
[ ] C-5 設定で上限を 3 にすると 4 語目以降が通知される                                        結果: (      )
```

## D. AND / OR（最優先）

```
初期値
[ ] D-1 設定 AND で一覧を新しく開く（URL に nrc_mode なし）→ AND が選択状態                     結果: (      )
[ ] D-2 設定を OR に変えて一覧を新しく開く → OR が選択状態                                    結果: (      )

AND / OR の結果
[ ] D-3 AND で「東京 営業」→ 両方を含むレコードだけが表示される                                結果: (      )
[ ] D-4 OR で「東京 営業」→ どちらかを含むレコードが表示される（D-3 より件数が多い）             結果: (      )
[ ] D-5 未検索の状態で AND/OR を切り替えても画面は再読み込みされない                            結果: (      )

検索中切替
[ ] D-6 AND で検索後に OR を押す → 同じ検索語のまま即再検索される                               結果: (      )
[ ] D-7 D-6 後: URL が nrc_mode=or、適用中表示が「/ OR（いずれかを含む）」、ボタンも OR          結果: (      )
[ ] D-8 逆方向（OR で検索後に AND）も同様に、URL・表示・ボタンがすべて AND になる               結果: (      )

再表示
[ ] D-9 OR で検索中にページング → OR のまま（AND に戻らない）                                  結果: (      )
[ ] D-10 OR で検索中にソート → OR のまま                                                     結果: (      )

クリア
[ ] D-11 OR で検索 → クリア → 検索語は解除され、AND/OR は OR のまま                            結果: (      )
[ ] D-12 設定画面の値は一覧での切り替えによって変わっていない（設定画面を開いて確認）             結果: (      )
```

## E. URL 復元（最優先）

```
[ ] E-1 検索後の URL に nrc_q と nrc_mode が含まれる                                         結果: (      )
[ ] E-2 F5 で再読み込み → 検索語が入力欄に、AND/OR がボタンに、適用中表示が復元される             結果: (      )
[ ] E-3 URL をコピーして別タブで開いても同じ状態になる                                         結果: (      )
[ ] E-4 URL の nrc_mode を xxx に書き換えて開く → 設定値のモードになる                          結果: (      )
[ ] E-5 v1.0.0 で検索した URL（nrc_mode なし）を開く → 設定値のモードで正常に表示される           結果: (      )
```

## F. 選択肢系（最優先）

```
[ ] F-1 ドロップダウン: 「新規」で絞り込まれる                                                結果: (      )
[ ] F-2 ラジオボタン: 「高」で絞り込まれる                                                   結果: (      )
[ ] F-3 チェックボックス: 「東京都」で絞り込まれる                                            結果: (      )
[ ] F-4 複数選択: 「重要」で絞り込まれる                                                     結果: (      )
[ ] F-5 部分一致にならない: 「東京」ではチェックボックス「東京都」に一致しない
        （文字列フィールドに「東京」を含むレコードだけが出る）                                  結果: (      )
[ ] F-6 クエリエラー（kintone のエラー画面）にならない                                         結果: (      )
[ ] F-7 従来機能: テーブル内フィールド（品名）・添付ファイル名で検索できる                        結果: (      )
```

## G. RICH_TEXT

```
[ ] G-1 リッチエディター内の語で絞り込まれる                                                  結果: (      )
[ ] G-2 装飾（太字・色）付きの文字列でも検索でき、クエリエラーにならない                          結果: (      )
```

## H. 条件生成不能語（次点）

設定で対象を「契約区分・優先度」だけにして確認する（確認後 0-2 の設定に戻す）。

```
[ ] H-1 「新規 営業」→ 検索は実行され、「検索中: 新規 / …」と表示される                          結果: (      )
[ ] H-2 同時に「一部の検索語は対象フィールドで検索できませんでした：営業」が表示される             結果: (      )
[ ] H-3 「営業」だけ → 画面は再読み込みされない                                               結果: (      )
[ ] H-4 同時に、検索できなかった旨がその場に表示される（「検索中:」とは表示されない）              結果: (      )
```

## I. snapshot 不一致（次点）

0-2 の設定後に、フォームで「備考」を削除し、「優先度」の選択肢「低」を削除してアプリを更新する（プラグイン設定は開かない）。

```
[ ] I-1 検索バーに「対象：5件（除外2件） ⚠ 一部除外」と表示される                               結果: (      )
[ ] I-2 パネルに「設定後に変更されたフィールドがあるため、一部を検索対象から除外しています。」       結果: (      )
[ ] I-3 パネルで 備考・優先度 が取り消し線＋「除外」、他は通常表示（使用中と除外中を識別できる）     結果: (      )
[ ] I-4 件数表示（5件＋除外2件）とチップの数（通常 5・除外 2）が一致している                       結果: (      )
[ ] I-5 この状態で検索してもエラーにならない（除外フィールドは query に含まれない。
        URL の query に 備考・優先度 が無いこと）                                             結果: (      )
[ ] I-6 プラグイン設定画面を開くと、消えたフィールドについて黄色の注意書きが出る                   結果: (      )
[ ] I-7 設定を保存し直すと、一覧の警告が消える                                                 結果: (      )
```

## J. 適用中表示（次点）

```
[ ] J-1 検索中: 「検索中: 東京 営業 / AND（すべて含む）」が検索バーの下に表示される                結果: (      )
[ ] J-2 OR の場合は「検索中: 東京 営業 / OR（いずれかを含む）」                                  結果: (      )
[ ] J-3 検索中はクリアボタンが表示される                                                      結果: (      )
[ ] J-4 未検索時: 適用中表示なし、クリアボタン非表示                                           結果: (      )
[ ] J-5 クリア → 絞り込み解除、入力欄が空、適用中表示とクリアボタンが消える                       結果: (      )
[ ] J-6 入力欄を空にして検索（Enter）でも解除される                                            結果: (      )
[ ] J-7 特殊文字（" と \ を含む語）で検索してもクエリエラーにならず、表示も崩れない                 結果: (      )
[ ] J-8 （参考）検索中に kintone 標準の絞り込みを変更したとき、「検索中:」表示が残るかをメモ        結果: (      )
```

## K. 0件

```
[ ] K-1 一致しない語で検索 → 「「…」に一致するレコードはありません。」が表示される                  結果: (      )
[ ] K-2 未検索でレコード 0 件の一覧では、このメッセージは出ない                                  結果: (      )
```

## L. 設定画面（次点）

```
[ ] L-1 設定画面がレイアウト崩れなく表示される。kintone 本体の余白・背景が変わらない                結果: (      )
[ ] L-2 候補: 文字列（1行／複数行）・リッチエディター・リンク・ドロップダウン・ラジオボタン・
        チェックボックス・複数選択・添付ファイルだけが出る（数値・日付・ユーザー選択は出ない）       結果: (      )
[ ] L-3 一致方法の列が「部分一致」「選択肢と完全一致」                                          結果: (      )
[ ] L-4 新規アプリでの初期値: 検索語の上限 = 5                                                 結果: (      )
[ ] L-5 「AND / OR の初期値」と表示され、
        「検索画面を開いたときの AND / OR の初期値を設定します。利用者は一覧画面上で切り替えられます。」 結果: (      )
[ ] L-6 保存 → プラグイン一覧に戻り保存メッセージが出る                                         結果: (      )
[ ] L-7 再表示 → チェック状態・初期値・上限が保存したとおりに復元される                           結果: (      )
[ ] L-8 既存設定保持: v1.0.0 で保存した設定（上限 3・OR など）が、更新後もそのまま表示・動作する    結果: (      )
[ ] L-9 未選択で保存 → エラー表示で保存されない。上限 11 → エラー表示                            結果: (      )
```

## M. a11y / UI

```
[ ] M-1 Tab で 検索欄 → AND → OR → 検索 →（クリア）→ 対象 の順に移動できる                       結果: (      )
[ ] M-2 フォーカス中の要素に枠（フォーカス表示）が見える                                         結果: (      )
[ ] M-3 AND / OR を Enter / Space で切り替えられる                                             結果: (      )
[ ] M-4 AND / OR にマウスを乗せると意味（すべて含む／いずれか含む）が表示される                     結果: (      )
[ ] M-5 「対象：N件」を Enter / Space で開閉、Esc・外側クリックで閉じる                           結果: (      )
[ ] M-6 幅 1280px: 検索バーが 1 行に収まり、kintone 標準のアイコン群を押し出さない                 結果: (      )
[ ] M-7 幅 1024px 程度でも極端に崩れない                                                       結果: (      )
[ ] M-8 全体を通して Console に赤字のエラーが出ていない                                          結果: (      )
```

## 後片付け

```
[ ] Z-1 H で変更した設定を 0-2 の状態に戻した                                                  結果: (      )
[ ] Z-2 I で削除したフィールド・選択肢を復元した（または検証アプリを破棄）                         結果: (      )
[ ] Z-3 環境を戻す場合は field-cross-search-plugin.zip（v1.0.0）を再読み込み
        （検索バーの AND|OR ボタンが消えれば戻っている）                                        結果: (      )
```

---

## 結果サマリ

| 区分 | 優先度 | 件数 | PASS | FAIL | 未確認 |
|---|---|---|---|---|---|
| 0 事前準備 | ― | 3 | | | |
| A 再発火 | 最優先 | 10 | | | |
| B IME | 最優先 | 4 | | | |
| C 5語 / 6語 | 次点 | 5 | | | |
| D AND / OR | 最優先 | 12 | | | |
| E URL 復元 | 最優先 | 5 | | | |
| F 選択肢系 | 最優先 | 7 | | | |
| G RICH_TEXT | その他 | 2 | | | |
| H 条件生成不能語 | 次点 | 4 | | | |
| I snapshot 不一致 | 次点 | 7 | | | |
| J 適用中表示 | 次点 | 8 | | | |
| K 0件 | その他 | 2 | | | |
| L 設定画面 | 次点 | 9 | | | |
| M a11y / UI | その他 | 8 | | | |
| 後片付け | ― | 3 | | | |

## FAIL 記録

```
項目:
操作:
期待結果:
実際の結果:
Console:
URL（query / nrc_q / nrc_mode）:
再現性:   ( 毎回 / 時々 / 1回のみ )
```

```
項目:
操作:
期待結果:
実際の結果:
Console:
URL（query / nrc_q / nrc_mode）:
再現性:   ( 毎回 / 時々 / 1回のみ )
```
